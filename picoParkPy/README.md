# Pico Park Clone – Python Desktop App

This bundles the original web-based Pico Park Clone into a native Python GUI using PyWebView.

## Setup

```bash
pip install -r requirements.txt
python app.py
```

A window will open running your game.
